# git-demo

## Commands

Clone repo (copy repo to local machine):

```bash
git clone <repo_url>
```

List changed files:

```bash
git status
```

Add file to be commited:

```bash
git add <file>
```

Commit changes:

```bash
git commit -m "<message>"
```
